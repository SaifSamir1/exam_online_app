


import 'package:flutter/material.dart';

class ExamDisplayError extends StatelessWidget {
  const ExamDisplayError({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Please Try Again ,something is wrong'),
    );
  }
}
